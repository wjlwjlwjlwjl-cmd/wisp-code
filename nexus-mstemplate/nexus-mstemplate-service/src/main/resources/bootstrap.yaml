spring:
  application:
    name: nexus-mstemplate
  profiles:
    active: ${RUN_ENV}
  cloud:
    nacos:
      discovery:
        username: nacos
        password: bite@123
        namespace: frameworkjava-${RUN_ENV}
        server-addr: ${NACOS_ADDR}
      config:
        username: nacos
        password: bite@123
        namespace: frameworkjava-${RUN_ENV}
        server-addr: ${NACOS_ADDR}
        file-extension: yaml
        # 共享配置
        shared-configs:
          - dataId: shared-redis-${RUN_ENV}.${spring.cloud.nacos.config.file-extension}
          - dataId: shared-caffeine-${RUN_ENV}.${spring.cloud.nacos.config.file-extension}
          - dataId: shared-rabbitmq-${RUN_ENV}.${spring.cloud.nacos.config.file-extension}
