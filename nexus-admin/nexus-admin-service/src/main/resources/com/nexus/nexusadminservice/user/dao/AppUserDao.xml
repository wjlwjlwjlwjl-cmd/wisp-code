<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexus.nexusadminservice.user.dao.AppUserDao">
    <select id="selectByEmail" parameterType="java.lang.String" resultType="com.nexus.nexusadminservice.user.domain.entity.AppUser">
        select *from app_user where email = #{email}
    </select>

    <select id="selectCount"
        parameterType="com.nexus.nexusadminapi.appuser.domain.dto.AppUserListReqDTO"
        resultType="java.lang.Long">
        select count(1) from app_user
        <where>
            <if test="userId != null and userId !='' ">
                and id = #{userId}
            </if>
            <if test="phoneNumber != null and phoneNumber !='' ">
                and phone_number = #{phoneNumber}
            </if>
            <if test="email != null and email !='' ">
                and email = #{email}
            </if>
            <if test="nickName != null and nickName !='' ">
                and nick_name like concat('%', #{nickName}, '%')
            </if>
        </where>
    </select>

    <select id="selectPage"
            parameterType="com.nexus.nexusadminapi.appuser.domain.dto.AppUserListReqDTO"
            resultType="com.nexus.nexusadminservice.user.domain.entity.AppUser">
        select * from app_user
        <where>
            <if test="userId != null and userId !='' ">
                and id = #{userId}
            </if>
            <if test="phoneNumber != null and phoneNumber !='' ">
                and phone_number = #{phoneNumber}
            </if>
            <if test="email != null and email !='' ">
                and email = #{email}
            </if>
            <if test="nickName != null and nickName !='' ">
                and nick_name like concat('%', #{nickName}, '%')
            </if>
        </where>
        order by id desc
        limit #{offset}, #{pageSize}
    </select>
</mapper>