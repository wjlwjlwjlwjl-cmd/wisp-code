<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.nexus.nexusadminservice.map.dao.MapDao">
    <!-- 对应接口 selectAllRegion 方法 -->
    <select id="selectAllRegion" resultType="com.nexus.nexusadminservice.map.domain.entity.SysRegion">
        SELECT * FROM sys_region order by pinyin
    </select>
</mapper>